<div class="track-crud-hint">
    <div class="track-crud-hint__icon" aria-hidden="true">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none">
            <path d="M12 16v-4M12 8h.01M22 12a10 10 0 1 1-20 0 10 10 0 0 1 20 0Z" stroke="currentColor" stroke-width="1.75" stroke-linecap="round"/>
        </svg>
    </div>
    <div class="track-crud-hint__body">
        <p class="track-crud-hint__title">Быстрее через загрузку файла</p>
        <p class="track-crud-hint__text">
            Для MP3/M4A используйте
            <a href="{{ path('admin_upload_track') }}">Загрузить треки</a>
            — метаданные, обложка и файл подставятся автоматически.
            Ручное создание нужно только для правки уже загруженных записей.
        </p>
    </div>
</div>
